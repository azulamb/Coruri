{
  "name": "Coruri",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "electron-packager src app --platform=all --arch=all --version=0.31.1 --overwrite --icon icon.icns",
    "start": "electron src",
    "release": "node release.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ntwitter": "^0.5.0"
  }
}
